version: "2"
linters:
  settings:
    paircheck:
      rules:
        - id: resource
          trigger: (*example.com/project/resource.Resource).Open
          satisfiers:
            - (*example.com/project/resource.Resource).Close
